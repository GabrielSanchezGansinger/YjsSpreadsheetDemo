# How to reproduce error.

Run:

```
npx y-websocket-server
npm run dev-concurrent
npx cypress open

```

Error does not happen when doing

```
npx y-websocket-server
npm run dev
npx cypres open

```
